$('#search_results').html("<%= escape_javascript(render "reservation_list") %>");
$('#sw_list').sweetPages({perPage:3});

var controls = $('.swControls').detach();
controls.appendTo('#search_results');
